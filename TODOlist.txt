
sources/Drawable.cpp:

45:       texture_buffer.isCreated() ; //TODO : add color buffer if useful ? 

sources/GUIWindow.cpp:

473://TODO : error handling
492://TODO : error handling
510://TODO : error handling
531://TODO : error handling
583://TODO : error handling
602://TODO : error handling
609:void GUIWindow::compute_projection(){ //TODO : complete uv projection method , implement baking
620:SDL_Surface* surf = ImageManager::project_uv_normals(instance->getCurrent().geometry , _UI.uv_width->value() , _UI.uv_height->value() , _UI.tangent_space->isChecked()); //TODO : change for managing the entire scene , maybe add scroll between different meshes 
718://TODO : bug here , greyscale -> prewitt -> sharpen -> undo -> sharpen -> prewitt ->  ... ? 

sources/ImageManager.cpp:

114://TODO : Add Tiling management when uv coordinates greater than limit
387://TODO : use multi threading for initialization and greyscale computing
751:normal = tan_space_transform(T , B , N , normal);//TODO : provide a better algorithm for tang space  

sources/TerminalOpt.cpp:

398:ImageManager::set_greyscale_luminance(images[id].first); //TODO change to hmap 

kernels/Kernel.cu:

236://TODO : case kernel < 0 
298://TODO : add custom kernels processing
